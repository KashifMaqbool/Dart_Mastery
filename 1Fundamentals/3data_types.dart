// Dart Numbers-> Integers
int marks = 24;

// Dart Numbers-> Double4
double pi = 3.14;

// Dart strings
String s = 'Allah';
var v = 'Muhammad';

// Dart Boolean
bool isValid = true;

// Dart lists
var list = [3,5,4];

// Dart Maps
var student = {'name':'Kashif','age':24, 'roll_no':59};

// Dart Runes
void main(){
    // unicode to string or characters
    var heart_symbol = '\u2665';
    var laugh_symbol = '\u{1f600}';
    print(heart_symbol);
    print(laugh_symbol);

    // string to unicode
    var a = 'a';
    var b = 'b';
    print(a.runes);
    print(b.runes);
}

/*                   Dart symbols
These are the objects used to refer and operator or identifier. */

/*                  Dart Dynamic Type
Dart is an optionally typed language. If the variable type is not specified explicitly, 
then the variable type is dynamic. */